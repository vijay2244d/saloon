<div class="barber-pole"></div>

<style>
.barber-pole {
  width: 60px;
  height: 300px;
  border: 4px solid #333;
  border-radius: 30px;
  overflow: hidden;
  box-shadow: 5px 5px 15px rgba(0,0,0,0.2);

  /* Combined gradients */
  background-image:
    linear-gradient(
      to right,
      rgba(0,0,0,0.3) 0%,
      rgba(255,255,255,0.4) 50%,
      rgba(0,0,0,0.3) 100%
    ),
    repeating-linear-gradient(
      45deg,
      #fff,
      #fff 20px,
      #ec1c24 20px,
      #ec1c24 40px,
      #fff 40px,
      #fff 60px,
      #0055a4 60px,
      #0055a4 80px
    );

  background-size: auto, 113px 113px;
  background-position: center, 0 0;

  animation: move-stripes 2s linear infinite;
}

@keyframes move-stripes {
  from {
    background-position: center, 0 0;
  }
  to {
    background-position: center, 0 113px;
  }
}
</style>
